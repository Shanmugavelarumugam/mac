// segregation_screen.dart
import 'package:flutter/material.dart';

class SegregationScreen extends StatelessWidget {
  const SegregationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Segregation")),
      body: const Center(child: Text("Segregation Screen Content")),
    );
  }
}
