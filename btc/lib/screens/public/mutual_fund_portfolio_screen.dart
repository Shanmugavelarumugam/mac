import 'package:flutter/material.dart';

class MutualFundPortfolioScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mutual Fund Portfolio")),
      body: Center(child: Text("Track your mutual fund portfolio here.")),
    );
  }
}
