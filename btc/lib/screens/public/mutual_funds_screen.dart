import 'package:flutter/material.dart';

class MutualFundsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mutual Funds")),
      body: Center(child: Text("Explore mutual funds information here.")),
    );
  }
}
