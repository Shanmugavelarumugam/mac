import 'package:flutter/material.dart';

class CheckMotorPolicyScreen extends StatelessWidget {
  const CheckMotorPolicyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Check Motor Policy Expiry')),
      body: const Center(child: Text('Check Motor Insurance Policy Expiry')),
    );
  }
}
