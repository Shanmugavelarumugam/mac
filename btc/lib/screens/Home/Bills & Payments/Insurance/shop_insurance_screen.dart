import 'package:flutter/material.dart';

class ShopInsuranceScreen extends StatelessWidget {
  const ShopInsuranceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Shop Insurance')),
      body: const Center(child: Text('Shop Insurance Coverage')),
    );
  }
}
