import 'package:flutter/material.dart';

class TeamLifeInsuranceScreen extends StatelessWidget {
  const TeamLifeInsuranceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Team Life Insurance')),
      body: const Center(child: Text('Team Life Insurance Details')),
    );
  }
}
