import 'package:flutter/material.dart';

class CyberInsuranceScreen extends StatelessWidget {
  const CyberInsuranceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cyber Insurance')),
      body: const Center(child: Text('Cyber Insurance Details')),
    );
  }
}
