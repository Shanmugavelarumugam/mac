import 'package:flutter/material.dart';

class ULIPScreen extends StatelessWidget {
  const ULIPScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ULIP')),
      body: const Center(child: Text('ULIP Details')),
    );
  }
}
