import 'package:flutter/material.dart';

class MutualFundsLoanScreen extends StatelessWidget {
  const MutualFundsLoanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mutual Funds Loan')),
      body: const Center(child: Text('Details about Mutual Funds Loan')),
    );
  }
}
