// upi_circle_screen.dart
import 'package:flutter/material.dart';

class UPICircleScreen extends StatelessWidget {
  const UPICircleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('UPI Circle')),
      body: Center(child: Text('UPI Circle Details')),
    );
  }
}
